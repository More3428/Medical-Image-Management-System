package com.example.MIMs.controller;

import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequestMapping("/edit")
public class AdminController {
    // Show tables from images in the filesystem 
    // to users in the database
    // create and admin service to handle deleting users and images. 
    //Resume project Tuesday March 25th
}
